{
  "name": "zero-cup-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verifiable World Cup Oracle — TEE-backed predictions committed on-chain before kickoff (0G Zero Cup)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "probe:tee": "tsx src/scripts/probe-tee.ts",
    "probe:x": "tsx src/scripts/probe-x.ts",
    "agent": "tsx src/agent/loop.ts",
    "dev": "tsx watch src/agent/loop.ts"
  },
  "dependencies": {
    "@0glabs/0g-serving-broker": "^0.7.8",
    "@0glabs/0g-ts-sdk": "^0.3.3",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
